{
  "name": "gulp-azure",
  "version": "1.0.0",
  "description": "basic tasks for azure node production",
  "keywords": "gulp, azure",
  "author": "Will Myers",
  "license": "ISC",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "gulp clean && gulp azure"
  },
  "devDependencies": {
    "gulp": "^3.9.0",
    "del": "^1.2.0"
  }
}
